pipeline {
    agent any
    stages {
        stage('Debug') {
            steps {
                sh'''
                sudo apt update
                sudo apt install python3-pip
                '''
            }
        }
    }
}

